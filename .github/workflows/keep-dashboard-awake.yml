name: Keep Dashboard Awake

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping-dashboard:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit dashboard
        run: |
          curl -L -A "Mozilla/5.0" --max-time 120 "https://fall-detection-yeyelna.streamlit.app/" || true

      - name: Ping Render backend
        run: |
          curl -L -A "Mozilla/5.0" --max-time 120 "https://wi-fi-rssi-fall-detection-system.onrender.com/health" || true
